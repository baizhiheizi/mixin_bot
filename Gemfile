# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

gem 'fastimage'
gem 'minitest-reporters'
gem 'minitest-stub-const'
gem 'pry'
gem 'puma' # dev-only: used by the blaze puma plugin tests
gem 'railties' # dev-only: test harness for the Rails generators
gem 'rake'
gem 'rdoc'
gem 'rubocop'
gem 'rubocop-rake'
gem 'simplecov'
gem 'webmock'
gem 'yard'
